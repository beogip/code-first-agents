# CLAUDE.md

## Cache busting

When modifying `assets/style.css`, bump the `?v=N` query string in all HTML files that reference it:

- `index.html`
- `patterns/deterministic-tools.html`
- `patterns/skill-orchestration.html`
